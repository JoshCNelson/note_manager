<% flash.each do |name, msg| %>
  <% if msg.is_a?(String) %>
    <div class="w-100 alert alert-<%= name == "notice" ? "warning" : "danger" %>">
      <a class="close" data-dismiss="alert">&#215;</a>
      <%= content_tag :div, msg, :id => "flash_#{name}" %>
    </div>
  <% end %>
<% end %>